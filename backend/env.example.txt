# Base de Datos
DATABASE_URL="postgresql://pepsico_user:postgres@localhost:5433/pepsico_fleet?schema=public"

# Servidor
PORT=3000
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Lista adicional de orígenes permitidos (separados por coma) para CORS
# Ejemplo: https://frontend-production-XXXX.up.railway.app,https://otro-dominio.com
# Usa * para permitir todos los orígenes (solo si sabes lo que haces)
ALLOWED_ORIGINS=

# JWT
JWT_SECRET=300719080102
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Email - OPCIÓN 1: SendGrid (MÁS RECOMENDADO - No requiere dominio)
# SendGrid es gratis hasta 100 emails/día y funciona perfectamente con Railway
# 1. Crea cuenta en: https://sendgrid.com
# 2. Obtén tu API Key en: Settings → API Keys → Create API Key
# 3. Verifica el email remitente en: Settings → Sender Authentication
# 4. Copia la API Key (formato: SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx...)
SENDGRID_API_KEY=SG.tu_api_key_aquí
SENDGRID_FROM_EMAIL=pepsicomanager@gmail.com

# Email - OPCIÓN 2: Resend (Requiere verificar dominio)
# Resend es gratis hasta 3,000 emails/mes pero requiere verificar un dominio
# 1. Crea cuenta en: https://resend.com
# 2. Obtén tu API Key en: Dashboard → API Keys → Create
# 3. Verifica tu dominio en: https://resend.com/domains
# 4. Copia la API Key (formato: re_xxxxxxxxxxxxx)
RESEND_API_KEY=re_tu_api_key_aquí
RESEND_FROM_EMAIL=noreply@tu-dominio-verificado.com

# Email - OPCIÓN 2: SMTP (Fallback si no usas Resend)
# Para generar una contraseña de aplicación de Gmail:
# 1. Ve a: https://myaccount.google.com/apppasswords
# 2. Selecciona "Correo" y "Otro (nombre personalizado)"
# 3. Escribe "PepsiCo Flota Sistema"
# 4. Copia la contraseña de 16 caracteres generada
# IMPORTANTE: Railway puede bloquear conexiones SMTP. Usa Resend si es posible.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=pepsicomanager@gmail.com
SMTP_PASS=genera_una_contraseña_de_aplicación_de_16_caracteres_aquí

# Cloudinary (Imágenes)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Configuración de Logs
LOG_LEVEL=info





